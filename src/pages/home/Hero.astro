<div
  id="canvas-holder"
  class="hidden lg:flex w-[calc(100dvw-90)] h-[120px] lg:h-[380px] overflow-hidden items-center justify-center mb-6"
>
</div>

<style>
  canvas {
    display: block;
  }
</style>

<script is:inline>
    let spaceX = 20,
      spaceY = 20,
      side = 10;
    let prevMouseX, prevMouseY;
  
    function setup() {
      let canvas = createCanvas(1200, 340);
      canvas.parent("canvas-holder");
      noStroke();
      prevMouseX = mouseX;
      prevMouseY = mouseY;
    }
  
    function draw() {
      background("#020c18");
  
      let smoothMouseX = lerp(prevMouseX, mouseX, 0.08);
      let smoothMouseY = lerp(prevMouseY, mouseY, 0.08);
  
      // Horizontal
      for (let x = 20; x < width; x += spaceX) {
        // Vertical
        for (let y = 20; y < height; y += spaceY) {
          let d = dist(x, y, smoothMouseX, smoothMouseY);
          if (d < 200) {
            fill("#bff8fe");
            side = map(d, 10, 90, 20, 5);
          } else {
            fill("#bff8fe");
            side = 1;
          }
  
          rect(x - side / 2, y - side / 2, side, side);
        }
      }
  
      prevMouseX = smoothMouseX;
      prevMouseY = smoothMouseY;
    }
  </script>
  
  